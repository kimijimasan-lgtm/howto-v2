# セキュリティ診断記録 — crossmemo.web.app

> Stripeセキュリティ申告書「脆弱性診断またはペネトレーションテストを定期的に実施し、必要な修正対応を行う」への根拠資料。

## 実施概要

| 項目 | 内容 |
|------|------|
| 対象 | https://crossmemo.web.app （PCスマホ連動メモ、Firebase Hosting） |
| 実施日 | 2026-07-03 |
| 実施方法 | 自動化ツール相当の簡易脆弱性診断（下記） |
| 実施者 | 開発者（Claude Code 支援） |
| 次回予定 | 3〜6か月ごと、または依存ライブラリ更新・大きな機能追加時 |

### 用いた診断手法（無料ツール相当）
1. **HTTPセキュリティヘッダー検査**（Mozilla Observatory / securityheaders.com 相当のチェックを `curl -I` で実施）
2. **CSP実効性のライブ検証**（実ブラウザで `securitypolicyviolation` イベントを収集し、全主要画面を操作して違反ゼロを確認）
3. **依存ライブラリのバージョン点検**（CDN固定版の既知脆弱性確認。`npm audit` は本番にサーバ側依存が無いため対象外）
4. **コードレベルのXSSレビュー**（入力値の表示経路・HTMLパース経路の監査）

---

## 1. 検出事項（診断前）

### 🔴 高: HTTPセキュリティヘッダーの欠落
`Strict-Transport-Security`（HSTS）は Firebase Hosting によりデフォルト付与されていたが、以下がすべて未設定だった。
- Content-Security-Policy（CSP）… 無し → XSS・データ流出の多層防御が不在
- X-Content-Type-Options … 無し → MIMEスニッフィング
- X-Frame-Options / frame-ancestors … 無し → クリックジャッキング
- Referrer-Policy … 無し → リファラ経由の情報漏えい
- Permissions-Policy … 無し → カメラ/マイク/位置情報等のAPI濫用面

### 🟡 中: XSS（保存型）につながり得るHTML処理（別途 app.js?v=863 で対応済み）
- 保存済みカード本文を `div.innerHTML = html` で解析していた（4箇所）。この方式は本文に `<img onerror=...>` 等があるとパース時点でスクリプトが発火し得る。
- 開発者用テンプレート更新モーダルでパネル名が未エスケープ（`${cat.name}`）。
- 添付ファイルカードがファイル名をインライン `onclick="fn('${file.name}')"` に文字列展開しており属性注入の余地。

---

## 2. 実施した修正

### セキュリティヘッダーの追加（`firebase.json`、2026-07-03デプロイ）
全レスポンス（`source: "**"`）に以下を付与：

| ヘッダー | 値（要旨） | 目的 |
|----------|-----------|------|
| Content-Security-Policy | `default-src 'self'` を基点に、必要な外部オリジン（gstatic / jsdelivr / cdnjs / apis.google / fonts / *.firebasedatabase.app(wss) / *.googleapis / youtube）のみ許可。`object-src 'none'` `base-uri 'self'` `form-action 'self'` `frame-ancestors 'none'` | スクリプト/通信/埋め込み先を許可リスト制御。多層XSS防御・データ流出防止 |
| X-Content-Type-Options | `nosniff` | MIMEスニッフィング防止 |
| X-Frame-Options | `DENY` | クリックジャッキング防止（frame-ancestorsの後方互換） |
| Referrer-Policy | `strict-origin-when-cross-origin` | リファラ漏えい抑止 |
| Permissions-Policy | `camera=(), microphone=(), geolocation=(), payment=(), usb=(), interest-cohort=()` | 不要ブラウザAPIを全面無効化 |

> CSPの `script-src` に `'unsafe-inline'` を含む点について: 本アプリは静的ホスティング上でインラインスクリプト（firebase設定・エラーハンドラ）とインラインstyleを多用するため、保守性を優先し許容した。インライン注入対策は下記のコード側多層防御（esc / DOMParser不活性解析 / ProseMirrorスキーマ）でカバーしている。外部スクリプト読み込み元は許可リストで厳格に制限済み。

### XSS対策の強化（`app.js?v=863`、2026-07-03デプロイ）
1. 保存済みHTMLの解析を、不活性ドキュメントでパースする `parseHTMLInert()`（`DOMParser`）に統一（4箇所）。パース時に `onerror` 等のイベントが実行されず画像読み込みも起きない。
2. テンプレート更新モーダルのパネル名を `esc()` でHTMLエスケープ。
3. 添付ファイルカードのインライン `onclick` を `addEventListener` に置換（属性注入を排除）。

### 既存の多層防御（診断で確認）
- テキスト補間は `esc()`（`&<>'"` をエンティティ化）を全表示箇所に適用済み。
- カード本文の描画は TipTap/ProseMirror のスキーマ経由で、未定義タグ・`on*`属性・`<script>` を自動除去。
- Realtime Database ルールにより他ユーザーのデータは読み書き不可（`users/$uid` は本人のみ、`templates/default` の書き込みは開発者メールのみ）。→ 保存型XSSの影響範囲は自アカウント内に限定。

---

## 3. 診断後の結果（エビデンス）

### セキュリティヘッダー（`curl -I https://crossmemo.web.app/`）
```
Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://www.gstatic.com https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://apis.google.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' data: https://fonts.gstatic.com; img-src 'self' data: blob: https:; connect-src 'self' https://*.firebasedatabase.app wss://*.firebasedatabase.app https://*.googleapis.com https://apis.google.com https://torisetu-234c3.firebaseapp.com; frame-src https://www.youtube.com https://www.youtube-nocookie.com https://apis.google.com https://torisetu-234c3.firebaseapp.com; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'
Strict-Transport-Security: max-age=31556926; includeSubDomains; preload
X-Content-Type-Options: nosniff
X-Frame-Options: DENY
Referrer-Policy: strict-origin-when-cross-origin
Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=(), usb=(), interest-cohort=()
```
→ 主要ヘッダーがすべて付与された（Observatory/securityheaders.com の減点項目を解消）。

### CSPのライブ検証（実ブラウザ、`securitypolicyviolation` を収集）
全主要画面を操作し、**CSP違反 0件**・機能停止なしを確認：
- ホーム画面: 認証セッション復元＋RTDBのライブデータ（パネル一覧・カード枚数）を正常描画
- 全文検索: 全カードを横断解析（新 `parseHTMLInert` 経由）し **749件ヒット**、ハイライト（`esc`経由）も正常
- カード編集画面: TipTap/ProseMirror（`.ProseMirror`）が正常描画（`hasEditor:true`）
- 収集結果: `window.__cspViolations` = `[]`（全操作を通じて空）

### XSSコードパスの単体検証（実ブラウザ）
- 新旧HTML解析の等価性を5ケースで比較 → **全ケース一致**
- `<img onerror>` を含むペイロードで、旧方式（`div.innerHTML`）は `onerror` 発火・新方式（`parseHTMLInert`）は**非発火**を確認

### 依存ライブラリ（CDN固定版、既知の重大脆弱性なし）
- Firebase JS SDK 10.12.0 / SortableJS 1.15.0 / html2pdf.js 0.10.1 / TipTap（自前バンドル）
- いずれもバージョン固定読み込み。サーバ側の実行依存は無く `npm audit` 対象外。

---

## 4. 残存リスク・今後の対応

- **CSP `script-src 'unsafe-inline'`**: インラインスクリプト由来の注入は防げない（＝コード側多層防御に依存）。将来インラインスクリプトを外部ファイル化できれば `'unsafe-inline'` 除去を検討。
- **`img-src https:`**: 画像は任意HTTPS元を許可（利便性優先）。画像はスクリプト実行面ではなく低リスク。
- **Stripe決済**: 決済情報は入力せずStripeホスト型ページ（buy.stripe.com）へ遷移する方式のため、当サイトはカード情報を一切扱わない（PCI範囲外）。

## 5. 申告書への回答根拠
本記録により、Stripe申告書「脆弱性診断またはペネトレーションテストを定期的に実施し、必要な修正対応を行う」に **「はい」** と回答可能。
- 実施: 2026-07-03（ヘッダー検査・CSPライブ検証・依存点検・XSSコードレビュー）
- 修正: セキュリティヘッダー追加、HTML解析の不活性化、エスケープ漏れ・属性注入の解消（`app.js?v=863` / `firebase.json`）
- 次回: 3〜6か月ごと、または依存更新・大規模改修時に再実施
